import { ListSearchPipe } from './list-search.pipe';

describe('ListSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
